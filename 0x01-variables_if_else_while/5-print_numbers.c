#include<stido.h>
/**
 * main - Entry value
 * Description: 'A program to print all base 10 numbers'
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
